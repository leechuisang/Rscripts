# Name all libraries required for script to run
list.of.packages <- c("ggplot2","ggpubr","stringr","RColorBrewer")

# Check and install all libraries in that list
i <- NULL
for (i in list.of.packages[(list.of.packages %in% installed.packages()[,"Package"])]){
  require(i, character.only = TRUE)
  }
